# Changelog

## 0.1.0

- Initial release.
- Capture provider request system prompts in `before_provider_request`.
- Add `custom` session entries for the initial system prompt and later changes.
- Store snapshots as `custom` entries so they stay out of model context.
